<snippet>
<content><![CDATA[
resource "aws_secretsmanager_secret_version" "aws_secretsmanager_secret_version_${1:suffix}" {
	secret_id = "${3:Required}"
	secret_string = "${4:Optional}"
	secret_binary = "${5:Optional}"
	version_stages = "${6:Optional}"
}
]]></content>
<tabTrigger>aws_secretsmanager_secret_version</tabTrigger>
<scope>source.terraform</scope>
</snippet>
