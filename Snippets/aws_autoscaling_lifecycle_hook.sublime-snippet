<snippet>
<content><![CDATA[
resource "aws_autoscaling_lifecycle_hook" "aws_autoscaling_lifecycle_hook_${1:suffix}" {
	name = "${3:Required}"
	autoscaling_group_name = "${4:Required}"
	default_result = "${5:Optional}"
	heartbeat_timeout = "${6:Optional}"
	notification_metadata = "${7:Optional}"
	notification_target_arn = "${8:Optional}"
	role_arn = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_autoscaling_lifecycle_hook</tabTrigger>
<scope>source.terraform</scope>
</snippet>
