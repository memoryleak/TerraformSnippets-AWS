<snippet>
<content><![CDATA[
resource "aws_ebs_volume" "aws_ebs_volume_${1:suffix}" {
	availability_zone = "${3:Required}"
	encrypted = "${4:Optional}"
	iops = "${5:Optional}"
	size = "${6:Optional}"
	type = "${7:Optional}"
	kms_key_id = "${8:Optional}"
	tags = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_ebs_volume</tabTrigger>
<scope>source.terraform</scope>
</snippet>
