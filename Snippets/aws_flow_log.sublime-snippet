<snippet>
<content><![CDATA[
resource "aws_flow_log" "aws_flow_log_${1:suffix}" {
	traffic_type = "${3:Required}"
	eni_id = "${4:Optional}"
	iam_role_arn = "${5:Optional}"
	log_destination = "${6:Optional}"
	log_group_name = "${7:Optional}"
	subnet_id = "${8:Optional}"
	vpc_id = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_flow_log</tabTrigger>
<scope>source.terraform</scope>
</snippet>
