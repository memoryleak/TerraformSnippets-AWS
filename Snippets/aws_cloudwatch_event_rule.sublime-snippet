<snippet>
<content><![CDATA[
resource "aws_cloudwatch_event_rule" "aws_cloudwatch_event_rule_${1:suffix}" {
	name = "${3:Optional}"
	name_prefix = "${4:Optional}"
	schedule_expression = "${5:Required}"
	event_pattern = "${6:Required}"
	description = "${7:Optional}"
	role_arn = "${8:Optional}"
	is_enabled = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_cloudwatch_event_rule</tabTrigger>
<scope>source.terraform</scope>
</snippet>
