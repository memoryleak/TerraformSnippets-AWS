<snippet>
<content><![CDATA[
resource "aws_kms_key" "aws_kms_key_${1:suffix}" {
	description = "${3:Optional}"
	key_usage = "${4:Optional}"
	policy = "${5:Optional}"
	deletion_window_in_days = "${6:Optional}"
	is_enabled = "${7:Optional}"
	enable_key_rotation = "${8:Optional}"
	tags = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_kms_key</tabTrigger>
<scope>source.terraform</scope>
</snippet>
