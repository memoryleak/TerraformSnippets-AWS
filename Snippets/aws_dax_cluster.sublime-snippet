<snippet>
<content><![CDATA[
resource "aws_dax_cluster" "aws_dax_cluster_${1:suffix}" {
	${2:iam_role_arn} = "${3:Required}"
	${4:replication_factor – (Required) The number of nodes in the DAX cluster. A replication factor of 1 will create a single} = "${5:Required}"
	${6:availability_zones} = "${7:Optional}"
	${8:server_side_encryption} = "${9:Optional}"
	${10:tags} = "${11:Optional}"
}
]]></content>
<tabTrigger>aws_dax_cluster</tabTrigger>
</snippet>
