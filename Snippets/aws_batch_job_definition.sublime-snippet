<snippet>
<content><![CDATA[
resource "aws_batch_job_definition" "aws_batch_job_definition_${1:suffix}" {
	${2:name} = "${3:Required}"
	${4:container_properties} = "${5:Optional}"
	${6:parameters} = "${7:Optional}"
	${8:retry_strategy} = "${9:Optional}"
	${10:timeout} = "${11:Optional}"
	${12:type} = "${13:Required}"
}
]]></content>
<tabTrigger>aws_batch_job_definition</tabTrigger>
</snippet>
