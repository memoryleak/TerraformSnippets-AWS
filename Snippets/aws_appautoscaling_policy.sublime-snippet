<snippet>
<content><![CDATA[
resource "aws_appautoscaling_policy" "aws_appautoscaling_policy_${1:suffix}" {
	name = "${3:Required}"
	policy_type = "${4:Optional}"
	resource_id = "${5:Required}"
	scalable_dimension = "${6:Required}"
	service_namespace = "${7:Required}"
	step_scaling_policy_configuration = "${8:Optional}"
	target_tracking_scaling_policy_configuration = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_appautoscaling_policy</tabTrigger>
<scope>source.terraform</scope>
</snippet>
