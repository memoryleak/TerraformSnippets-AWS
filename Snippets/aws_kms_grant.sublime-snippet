<snippet>
<content><![CDATA[
resource "aws_kms_grant" "aws_kms_grant_${1:suffix}" {
	name = "${3:Optional}"
	key_id = "${4:Required}"
	grantee_principal = "${5:Required}"
	operations = "${6:Required}"
	retiree_principal = "${7:Optional}"
	constraints = "${8:Optional}"
	grant_creation_tokens = "${9:Optional}"
	retire_on_delete = "${10:Required}"
}
]]></content>
<tabTrigger>aws_kms_grant</tabTrigger>
<scope>source.terraform</scope>
</snippet>
