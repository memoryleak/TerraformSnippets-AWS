<snippet>
<content><![CDATA[
resource "aws_alb_listener" "aws_alb_listener_${1:suffix}" {
	load_balancer_arn = "${3:Required}"
	port = "${4:Required}"
	protocol = "${5:Optional}"
	ssl_policy = "${6:Optional}"
	certificate_arn = "${7:Optional}"
	default_action = "${8:Required}"
}
]]></content>
<tabTrigger>aws_alb_listener</tabTrigger>
<scope>source.terraform</scope>
</snippet>
