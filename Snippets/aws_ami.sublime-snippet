<snippet>
<content><![CDATA[
resource "aws_ami" "aws_ami_${1:suffix}" {
	ena_support = "${3:Optional}"
	root_device_name = "${4:Optional}"
	virtualization_type = "${5:Optional}"
	architecture = "${6:Optional}"
	ebs_block_device = "${7:Optional}"
	ephemeral_block_device = "${8:Optional}"
	tags = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_ami</tabTrigger>
<scope>source.terraform</scope>
</snippet>
