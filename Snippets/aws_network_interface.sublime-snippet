<snippet>
<content><![CDATA[
resource "aws_network_interface" "aws_network_interface_${1:suffix}" {
	subnet_id = "${3:Required}"
	description = "${4:Optional}"
	private_ips = "${5:Optional}"
	private_ips_count = "${6:Optional}"
	security_groups = "${7:Optional}"
	attachment = "${8:Optional}"
	source_dest_check = "${9:Optional}"
	tags = "${10:Optional}"
}
]]></content>
<tabTrigger>aws_network_interface</tabTrigger>
<scope>source.terraform</scope>
</snippet>
