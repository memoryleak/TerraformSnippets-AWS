<snippet>
<content><![CDATA[
resource "aws_db_subnet_group" "aws_db_subnet_group_${1:suffix}" {
	name = "${3:Optional}"
	name_prefix = "${4:Optional}"
	description = "${5:Optional}"
	subnet_ids = "${6:Required}"
	tags = "${7:Optional}"
}
]]></content>
<tabTrigger>aws_db_subnet_group</tabTrigger>
<scope>source.terraform</scope>
</snippet>
