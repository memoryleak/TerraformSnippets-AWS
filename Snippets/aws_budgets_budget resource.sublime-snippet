<snippet>
<content><![CDATA[
resource "aws_budgets_budget resource" "aws_budgets_budget resource_${1:suffix}" {
	account_id = "${3:Optional}"
	name = "${4:Optional}"
	name_prefix = "${5:Optional}"
	budget_type = "${6:Required}"
	cost_filters = "${7:Optional}"
	cost_types = "${8:Optional}"
	limit_amount = "${9:Required}"
	limit_unit = "${10:Required}"
	time_unit = "${11:Required}"
}
]]></content>
<tabTrigger>aws_budgets_budget resource</tabTrigger>
<scope>source.terraform</scope>
</snippet>
