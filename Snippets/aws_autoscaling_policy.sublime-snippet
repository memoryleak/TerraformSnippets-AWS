<snippet>
<content><![CDATA[
resource "aws_autoscaling_policy" "aws_autoscaling_policy_${1:suffix}" {
	name = "${3:Required}"
	autoscaling_group_name = "${4:Required}"
	adjustment_type = "${5:Optional}"
	policy_type = "${6:Optional}"
	estimated_instance_warmup = "${7:Optional}"
}
]]></content>
<tabTrigger>aws_autoscaling_policy</tabTrigger>
<scope>source.terraform</scope>
</snippet>
