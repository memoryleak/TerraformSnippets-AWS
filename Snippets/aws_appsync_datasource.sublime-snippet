<snippet>
<content><![CDATA[
resource "aws_appsync_datasource" "aws_appsync_datasource_${1:suffix}" {
	api_id = "${3:Required}"
	type = "${4:Required}"
	description = "${5:Optional}"
	service_role_arn = "${6:Optional}"
	dynamodb_config = "${7:Optional}"
	elasticsearch_config = "${8:Optional}"
	http_config = "${9:Optional}"
	lambda_config = "${10:Optional}"
}
]]></content>
<tabTrigger>aws_appsync_datasource</tabTrigger>
<scope>source.terraform</scope>
</snippet>
