<snippet>
<content><![CDATA[
resource "aws_iam_policy_attachment" "aws_iam_policy_attachment_${1:suffix}" {
	name (Required) = "${3:Required}"
	users (Optional) = "${4:Required}"
	roles (Optional) = "${5:Required}"
	groups (Optional) = "${6:Required}"
	policy_arn (Required) = "${7:Required}"
}
]]></content>
<tabTrigger>aws_iam_policy_attachment</tabTrigger>
<scope>source.terraform</scope>
</snippet>
