<snippet>
<content><![CDATA[
resource "aws_ssm_document resource" "aws_ssm_document resource_${1:suffix}" {
	name = "${3:Required}"
	content = "${4:Required}"
	document_format = "${5:Optional}"
	document_type = "${6:Required}"
	permissions = "${7:Optional}"
	tags = "${8:Optional}"
}
]]></content>
<tabTrigger>aws_ssm_document resource</tabTrigger>
<scope>source.terraform</scope>
</snippet>
