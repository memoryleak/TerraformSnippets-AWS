<snippet>
<content><![CDATA[
resource "aws_s3_bucket_inventory" "aws_s3_bucket_inventory_${1:suffix}" {
	bucket = "${3:Required}"
	name = "${4:Required}"
	included_object_versions = "${5:Required}"
	schedule = "${6:Required}"
	destination = "${7:Required}"
	enabled = "${8:Optional}"
	filter = "${9:Optional}"
	optional_fields = "${10:Optional}"
}
]]></content>
<tabTrigger>aws_s3_bucket_inventory</tabTrigger>
<scope>source.terraform</scope>
</snippet>
