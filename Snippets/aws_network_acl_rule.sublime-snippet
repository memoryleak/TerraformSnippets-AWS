<snippet>
<content><![CDATA[
resource "aws_network_acl_rule" "aws_network_acl_rule_${1:suffix}" {
	network_acl_id = "${3:Required}"
	rule_number = "${4:Required}"
	egress = "${5:Optional}"
	rule_action = "${6:Required}"
	cidr_block = "${7:Optional}"
	ipv6_cidr_block = "${8:Optional}"
	from_port = "${9:Optional}"
	to_port = "${10:Optional}"
}
]]></content>
<tabTrigger>aws_network_acl_rule</tabTrigger>
<scope>source.terraform</scope>
</snippet>
