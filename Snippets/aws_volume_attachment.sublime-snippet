<snippet>
<content><![CDATA[
resource "aws_volume_attachment" "aws_volume_attachment_${1:suffix}" {
	device_name = "${3:Required}"
	instance_id = "${4:Required}"
	volume_id = "${5:Required}"
	force_detach = "${6:Optional}"
	skip_destroy = "${7:Optional}"
}
]]></content>
<tabTrigger>aws_volume_attachment</tabTrigger>
<scope>source.terraform</scope>
</snippet>
