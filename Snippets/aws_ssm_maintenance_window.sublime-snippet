<snippet>
<content><![CDATA[
resource "aws_ssm_maintenance_window" "aws_ssm_maintenance_window_${1:suffix}" {
	name = "${3:Required}"
	schedule = "${4:Required}"
	cutoff = "${5:Required}"
	duration = "${6:Required}"
	allow_unassociated_targets = "${7:Optional}"
	enabled = "${8:Optional}"
	schedule_timezone = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_ssm_maintenance_window</tabTrigger>
<scope>source.terraform</scope>
</snippet>
