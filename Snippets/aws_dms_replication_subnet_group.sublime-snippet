<snippet>
<content><![CDATA[
resource "aws_dms_replication_subnet_group" "aws_dms_replication_subnet_group_${1:suffix}" {
	replication_subnet_group_description = "${3:Required}"
	replication_subnet_group_id = "${4:Required}"
	subnet_ids = "${5:Required}"
	tags = "${6:Optional}"
}
]]></content>
<tabTrigger>aws_dms_replication_subnet_group</tabTrigger>
<scope>source.terraform</scope>
</snippet>
