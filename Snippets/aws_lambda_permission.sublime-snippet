<snippet>
<content><![CDATA[
resource "aws_lambda_permission" "aws_lambda_permission_${1:suffix}" {
	action = "${3:Required}"
	event_source_token = "${4:Optional}"
	function_name = "${5:Required}"
	principal = "${6:Required}"
	source_account = "${7:Optional}"
	source_arn = "${8:Optional}"
	statement_id = "${9:Optional}"
	statement_id_prefix = "${10:Optional}"
}
]]></content>
<tabTrigger>aws_lambda_permission</tabTrigger>
<scope>source.terraform</scope>
</snippet>
