<snippet>
<content><![CDATA[
resource "aws_elasticache_cluster" "aws_elasticache_cluster_${1:suffix}" {
	replication_group_id = "${3:Optional}"
	apply_immediately = "${4:Optional}"
	snapshot_arns – (Optional) A single = "${5:Required}"
	snapshot_name = "${6:Optional}"
	snapshot_retention_limit = "${7:Optional}"
	preferred_availability_zones = "${8:Optional}"
	tags = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_elasticache_cluster</tabTrigger>
<scope>source.terraform</scope>
</snippet>
