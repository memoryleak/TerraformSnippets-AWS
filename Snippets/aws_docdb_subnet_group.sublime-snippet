<snippet>
<content><![CDATA[
resource "aws_docdb_subnet_group" "aws_docdb_subnet_group_${1:suffix}" {
	${2:name} = "${3:Optional}"
	${4:name_prefix} = "${5:Optional}"
	${6:description} = "${7:Optional}"
	${8:subnet_ids} = "${9:Required}"
	${10:tags} = "${11:Optional}"
}
]]></content>
<tabTrigger>aws_docdb_subnet_group</tabTrigger>
</snippet>
