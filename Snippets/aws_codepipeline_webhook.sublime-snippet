<snippet>
<content><![CDATA[
resource "aws_codepipeline_webhook" "aws_codepipeline_webhook_${1:suffix}" {
	name = "${3:Required}"
	authentication = "${4:Required}"
	authentication_configuration = "${5:Optional}"
	target_action = "${6:Required}"
	target_pipeline = "${7:Required}"
}
]]></content>
<tabTrigger>aws_codepipeline_webhook</tabTrigger>
<scope>source.terraform</scope>
</snippet>
