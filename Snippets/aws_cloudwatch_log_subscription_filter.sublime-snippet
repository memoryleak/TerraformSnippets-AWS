<snippet>
<content><![CDATA[
resource "aws_cloudwatch_log_subscription_filter" "aws_cloudwatch_log_subscription_filter_${1:suffix}" {
	name = "${3:Required}"
	destination_arn = "${4:Required}"
	filter_pattern = "${5:Required}"
	log_group_name = "${6:Required}"
	role_arn = "${7:Optional}"
	distribution = "${8:Optional}"
}
]]></content>
<tabTrigger>aws_cloudwatch_log_subscription_filter</tabTrigger>
<scope>source.terraform</scope>
</snippet>
