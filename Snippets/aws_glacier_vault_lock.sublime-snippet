<snippet>
<content><![CDATA[
resource "aws_glacier_vault_lock" "aws_glacier_vault_lock_${1:suffix}" {
	complete_lock = "${3:Required}"
	policy = "${4:Required}"
	vault_name = "${5:Required}"
	ignore_deletion_error = "${6:Optional}"
}
]]></content>
<tabTrigger>aws_glacier_vault_lock</tabTrigger>
<scope>source.terraform</scope>
</snippet>
