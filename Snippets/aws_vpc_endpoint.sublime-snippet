<snippet>
<content><![CDATA[
resource "aws_vpc_endpoint" "aws_vpc_endpoint_${1:suffix}" {
	vpc_id = "${3:Required}"
	vpc_endpoint_type = "${4:Optional}"
	service_name = "${5:Required}"
	auto_accept = "${6:Optional}"
	policy = "${7:Optional}"
	route_table_ids = "${8:Optional}"
	subnet_ids = "${9:Optional}"
	security_group_ids = "${10:Optional}"
	private_dns_enabled = "${11:Optional}"
}
]]></content>
<tabTrigger>aws_vpc_endpoint</tabTrigger>
<scope>source.terraform</scope>
</snippet>
