<snippet>
<content><![CDATA[
resource "aws_ec2_transit_gateway_vpc_attachment" "aws_ec2_transit_gateway_vpc_attachment_${1:suffix}" {
	subnet_ids = "${3:Required}"
	transit_gateway_id = "${4:Required}"
	vpc_id = "${5:Required}"
	dns_support = "${6:Optional}"
	ipv6_support = "${7:Optional}"
	transit_gateway_default_route_table_association = "${8:Optional}"
	transit_gateway_default_route_table_propagation = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_ec2_transit_gateway_vpc_attachment</tabTrigger>
<scope>source.terraform</scope>
</snippet>
