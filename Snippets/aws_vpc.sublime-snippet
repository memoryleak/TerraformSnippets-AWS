<snippet>
<content><![CDATA[
resource "aws_vpc" "aws_vpc_${1:suffix}" {
	cidr_block = "${3:Required}"
	instance_tenancy = "${4:Optional}"
	enable_dns_support = "${5:Optional}"
	enable_dns_hostnames = "${6:Optional}"
	enable_classiclink = "${7:Optional}"
	enable_classiclink_dns_support = "${8:Optional}"
	tags = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_vpc</tabTrigger>
<scope>source.terraform</scope>
</snippet>
