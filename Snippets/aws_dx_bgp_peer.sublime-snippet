<snippet>
<content><![CDATA[
resource "aws_dx_bgp_peer" "aws_dx_bgp_peer_${1:suffix}" {
	address_family = "${3:Required}"
	bgp_asn = "${4:Required}"
	virtual_interface_id = "${5:Required}"
	amazon_address = "${6:Optional}"
	bgp_auth_key = "${7:Optional}"
	customer_address = "${8:Optional}"
}
]]></content>
<tabTrigger>aws_dx_bgp_peer</tabTrigger>
<scope>source.terraform</scope>
</snippet>
