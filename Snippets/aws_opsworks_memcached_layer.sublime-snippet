<snippet>
<content><![CDATA[
resource "aws_opsworks_memcached_layer" "aws_opsworks_memcached_layer_${1:suffix}" {
	stack_id = "${3:Required}"
	allocated_memory = "${4:Optional}"
	auto_assign_elastic_ips = "${5:Optional}"
	auto_assign_public_ips = "${6:Optional}"
	custom_instance_profile_arn = "${7:Optional}"
	custom_security_group_ids = "${8:Optional}"
	install_updates_on_boot = "${9:Optional}"
	instance_shutdown_timeout = "${10:Optional}"
	elastic_load_balancer = "${11:Optional}"
	drain_elb_on_shutdown = "${12:Optional}"
	system_packages = "${13:Optional}"
	ebs_volume = "${14:Optional}"
	custom_json = "${15:Optional}"
}
]]></content>
<tabTrigger>aws_opsworks_memcached_layer</tabTrigger>
<scope>source.terraform</scope>
</snippet>
