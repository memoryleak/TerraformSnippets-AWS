<snippet>
<content><![CDATA[
resource "aws_opsworks_rds_db_instance" "aws_opsworks_rds_db_instance_${1:suffix}" {
	stack_id = "${3:Required}"
	rds_db_instance_arn = "${4:Required}"
	db_user = "${5:Required}"
	db_password = "${6:Required}"
}
]]></content>
<tabTrigger>aws_opsworks_rds_db_instance</tabTrigger>
<scope>source.terraform</scope>
</snippet>
