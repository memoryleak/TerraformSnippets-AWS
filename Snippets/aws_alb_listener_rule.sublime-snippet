<snippet>
<content><![CDATA[
resource "aws_alb_listener_rule" "aws_alb_listener_rule_${1:suffix}" {
	${2:listener_arn} = "${3:Required}"
	${4:priority} = "${5:Optional}"
	${6:action} = "${7:Required}"
	${8:condition} = "${9:Required}"
}
]]></content>
<tabTrigger>aws_alb_listener_rule</tabTrigger>
</snippet>
