<snippet>
<content><![CDATA[
resource "aws_subnet" "aws_subnet_${1:suffix}" {
	availability_zone = "${3:Optional}"
	availability_zone_id = "${4:Optional}"
	cidr_block = "${5:Required}"
	ipv6_cidr_block = "${6:Optional}"
	map_public_ip_on_launch = "${7:Optional}"
	assign_ipv6_address_on_creation = "${8:Optional}"
	vpc_id = "${9:Required}"
	tags = "${10:Optional}"
}
]]></content>
<tabTrigger>aws_subnet</tabTrigger>
<scope>source.terraform</scope>
</snippet>
