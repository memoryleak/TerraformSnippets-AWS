<snippet>
<content><![CDATA[
resource "aws_codecommit_trigger" "aws_codecommit_trigger_${1:suffix}" {
	repository_name = "${3:Required}"
	name = "${4:Required}"
	destination_arn = "${5:Required}"
	custom_data = "${6:Optional}"
	branches = "${7:Optional}"
	events = "${8:Required}"
}
]]></content>
<tabTrigger>aws_codecommit_trigger</tabTrigger>
<scope>source.terraform</scope>
</snippet>
