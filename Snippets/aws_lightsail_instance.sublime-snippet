<snippet>
<content><![CDATA[
resource "aws_lightsail_instance" "aws_lightsail_instance_${1:suffix}" {
	name = "${3:Required}"
	availability_zone = "${4:Required}"
	blueprint_id = "${5:Required}"
	bundle_id = "${6:Required}"
	key_pair_name = "${7:Required}"
	user_data = "${8:Optional}"
}
]]></content>
<tabTrigger>aws_lightsail_instance</tabTrigger>
<scope>source.terraform</scope>
</snippet>
