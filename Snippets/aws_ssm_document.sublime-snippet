<snippet>
<content><![CDATA[
resource "aws_ssm_document" "aws_ssm_document_${1:suffix}" {
	name = "${3:Required}"
	content = "${4:Required}"
	document_format = "${5:Optional}"
	document_type = "${6:Required}"
	permissions = "${7:Optional}"
	tags = "${8:Optional}"
}
]]></content>
<tabTrigger>aws_ssm_document</tabTrigger>
<scope>source.terraform</scope>
</snippet>
