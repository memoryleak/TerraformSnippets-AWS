<snippet>
<content><![CDATA[
resource "aws_secretsmanager_secret" "aws_secretsmanager_secret_${1:suffix}" {
	name_prefix = "${3:Optional}"
	description = "${4:Optional}"
	kms_key_id = "${5:Optional}"
	policy = "${6:Optional}"
	recovery_window_in_days = "${7:Optional}"
	rotation_lambda_arn = "${8:Optional}"
	rotation_rules = "${9:Optional}"
}
]]></content>
<tabTrigger>aws_secretsmanager_secret</tabTrigger>
<scope>source.terraform</scope>
</snippet>
