<snippet>
<content><![CDATA[
resource "aws_secretsmanager_secret" "aws_secretsmanager_secret_${1:suffix}" {
	${2:name_prefix} = "${3:value}"
	${4:description} = "${5:value}"
	${6:kms_key_id} = "${7:value}"
	${8:policy} = "${9:value}"
	${10:recovery_window_in_days} = "${11:value}"
	${12:rotation_lambda_arn} = "${13:value}"
	${14:rotation_rules} = "${15:value}"
}
]]></content>
<tabTrigger>aws_secretsmanager_secret</tabTrigger>
</snippet>
