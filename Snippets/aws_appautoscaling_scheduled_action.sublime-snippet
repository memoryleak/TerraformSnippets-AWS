<snippet>
<content><![CDATA[
resource "aws_appautoscaling_scheduled_action" "aws_appautoscaling_scheduled_action_${1:suffix}" {
	${2:name} = "${3:Required}"
	${4:service_namespace} = "${5:Required}"
	${6:resource_id} = "${7:Required}"
	${8:scalable_dimension} = "${9:Optional}"
	${10:scalable_target_action} = "${11:Optional}"
}
]]></content>
<tabTrigger>aws_appautoscaling_scheduled_action</tabTrigger>
</snippet>
